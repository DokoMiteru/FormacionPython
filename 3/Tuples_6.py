mi_tuple =(1,2,(10,20),4)
 
#=== asignación se puede realizar por que tienen el mismo  numero de elemento y variables.
t=(1,2,3,1)
#x,y,z = t
#print(z,t,z)
print(t.count(1))
#print(type(mi_tuples)) > en la variable se puede omitir los parentesis del principio y del final y siguiria siendo un tuples.
#print(mi_tuple[2][0])